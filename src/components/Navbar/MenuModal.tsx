import React from 'react'
import { Modal } from 'antd'

type MenuModalProps = {
  open: boolean
  onClose: () => void
}

const MenuModal = () => {
  return <Modal></Modal>
}

export default MenuModal
